# Exact versions locked for Windows 7 SP1 64-bit + Python 3.8.10
# Audit date: 2026-09-08
# Do NOT upgrade without re-verifying Win7 compatibility.

# Core
requests==2.27.1          # last 2.x fully tested on older OpenSSL/Win7
urllib3==1.26.15          # compatible with requests 2.27
certifi==2021.10.8
charset-normalizer==2.0.12
idna==3.3
websocket-client==1.3.3   # pure-python, no modern SSL requirement
python-dateutil==2.8.2
pytz==2022.1
six==1.16.0

# Crypto / HMAC
# hashlib is stdlib

# Data
numpy==1.21.6             # last numpy with official Win7 wheels for py3.8
pandas==1.3.5             # last stable with numpy 1.21 + py3.8
scipy==1.7.3

# ML - CPU only, Win7 compatible
scikit-learn==1.0.2       # last 1.0.x, pure CPU, no modern Windows API
joblib==1.1.0
threadpoolctl==3.1.0
xgboost==1.5.2            # last 1.5.x with known Win7 binary support; CPU only

# Persistence / config
cryptography==3.4.8       # older OpenSSL binding, Win7 friendly
PyYAML==6.0
python-dotenv==0.19.2

# Testing
pytest==7.0.1
pytest-cov==3.0.0
responses==0.17.0
freezegun==1.1.0

# GUI: tkinter is stdlib on Windows Python installer
# No extra GUI deps

# Avoid:
# - TensorFlow / PyTorch (modern builds require Win10+)
# - LightGBM / CatBoost (binary compatibility not verified for Win7)
# - any package requiring Windows 10+ APIs or VS2019+ runtime that breaks on Win7
